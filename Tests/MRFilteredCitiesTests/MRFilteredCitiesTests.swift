    import XCTest
    @testable import MRFilteredCities

    final class MRFilteredCitiesTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(MRFilteredCities().text, "Hello, World!")
        }
    }
